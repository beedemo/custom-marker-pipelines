#!groovy
pipeline {
    options { 
        buildDiscarder(logRotator(numToKeepStr: '7')) 
        skipDefaultCheckout() 
    }
    stages {
        stage('Setup') {
			steps {
				script {
  					dockerBuildPublish {
						dockerHubCredentialsId = 'docker-hub-beedemo'
					}
				}
			}
		}
	}
}
